# 配置作用域仓库 - 只有@yourcompany包使用内部仓库
@yourcompany:registry=https://npm.yourcompany.com/

# 默认使用公共npm仓库
registry=https://registry.npmjs.org/

# 发布配置 - 仅对@yourcompany包生效
@yourcompany:publish-config.registry=https://npm.yourcompany.com/
@yourcompany:publish-config.access=restricted

# 安全配置
audit-level=moderate
fund=false